name: Windows build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  BUILD_TYPE: Release
  TARGET: FightClubGame

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Download VCPKG
      run: ./Automation/downloadVCPKG.bat

    - name: Install packages
      run: ./Automation/installPackages.bat


    - name: Configure CMake
      run: ./Automation/generate_project_files.bat

    - name: Build
      run: ./Automation/build_release.bat

    - name: Test
      run: ./Automation/test.bat

    
